#include <stdio.h>
int main(int argc, char *argv[])
{
    puts("Hello world.");
    puts("More Text to print in C!");
    puts("This isn't so bad right now.");
    puts("A little different than C++ though."); 
    return 0;
}
